# Session handoff

Startup recovery (2026-09-12): the local URL refused connections because no server was listening on port 8765. `main` was clean and matched `origin/main` at the prior PDF merge `615997c`. A detached background server restored HTTP 200 with all 64 Calculator fields. Development branch `fix/windows-launcher` adds `Start-Estimator.cmd` and `Start-Estimator.ps1`, includes both in the distribution, and documents double-click startup after a Windows restart.

Launcher verification: Windows PowerShell 5.1 parsed and executed the scripts. The extracted 17-file distribution started from a directory containing spaces while the invoking shell was in another directory; the server remained available after that shell exited. Repeated launch reused the same process. An unrelated occupied port was refused without stopping it; missing runtime/dependencies produced an actionable error. Runtime discovery selected the installed bundled ReportLab 4.4.9 instead of a different system version. The CMD wrapper reused the real app, and saved quote/settings content hashes were unchanged across these launcher checks. All 47 application tests passed with original workbook reconstruction enabled; JavaScript syntax, build and diff checks passed. Temporary launch smoke files/logs are ignored under `.runtime`; no login task or public deployment was created.

Working checkout: `C:\ESTIMATOR\app`. Sources remain at `C:\ESTIMATOR\Quote.xlsm` and `C:\ESTIMATOR\Inventory_list.xlsm`. Root was not a Git repository; its files were preserved. Remote baseline was `origin/main` at `18d5058`, with no open PRs or other remote branches at inspection.

Reporting development branch: `feat/branded-quote-pdf`, started from reconciled `main` / `origin/main` at `805eac1`. The prior Calculator implementation `d084062` was merged through PR #1 as `805eac141d72a0c807cb9065ce1b0d6a9450013a`. The repository includes runtime code, imported data, UI, independent Excel fixtures, extraction/capture scripts, tests, architecture and scope documentation. Feature publication and CI results are recorded in GitHub PRs and commit history.

Run: install `python -m pip install -r requirements.txt`, then `python -m estimator` from the checkout. Default URL is `http://127.0.0.1:8765`. Data is local SQLite at `.runtime/estimator.sqlite3`. Build: `python scripts/build.py`; archive goes to ignored `dist/` and includes the runtime requirements file and original logo.

Checks: install `python -m pip install -r requirements-dev.txt`, then run `python -m unittest discover -s tests -v`, `node --check static/app.js`, `python scripts/build.py`. Set `ESTIMATOR_WORKBOOK_DIR=C:\ESTIMATOR` to include both source-reconstruction tests. Without source files, those two optional checks skip but all Excel scenario fixtures remain required.

Prior Calculator parity validation: all 36 tests passed with both original source workbooks included. JavaScript syntax and Python distribution build passed. Browser checks exercised all 64 visible input controls, decimal spray adjustments (total 3767.746652, displayed $3,767.75), quote saving, supplier price 100 and markup 40% (sell price 140), preserved saved pricing, and explicit repricing (total 4689.450842, displayed $4,689.45). This is evidence of the checked scenarios, not a claim of original XLSM live-link execution or public deployment.

Reporting work: the supplied logo replaces the invented browser branding; Download PDF requests a server-generated attachment. Saved, unchanged quotes use the stored-report endpoint, while new/edited estimates use captured current inputs and pricing. ReportLab 4.4.9 supplies rendering; pypdf is used by report tests. The download disables duplicate requests, validates status/content type/file size and retains the captured quote identity if edits occur while the request is pending.

Reporting validation (2026-09-07): all 47 tests passed, including both original workbook reconstruction checks and all 216 Excel scenarios. The report's 29 non-overlapping charges reconcile with all 206 scenarios that have numeric Excel grand totals. The combined-adjustments report displays $147,070.68, from unrounded 147070.6835345901. New tests inspect actual PDF text, embedded fonts/logo, numeric tokens, long notes, escaped markup, complete error lists, old snapshots, read-only previews, source lineage, response headers and HTTP origin restrictions. JavaScript syntax and distribution build passed. An extracted distribution independently rendered a PDF using only its packaged modules, data, logo and installed dependencies.

Visual checks covered every page of five current-renderer samples (30 pages): all costs with adjustments (5), a long title/notes/word fixture (9), default zero quantities (5), zero daily output (6), and zero project area (5). Decimal quantities remain intact; no clipping or overlapping content was observed. Zero daily output reports all 26 recorded errors; zero area preserves the $1,260 total while marking only its rate unavailable. Browser checks confirmed the original 5375×1790 image loads, missing titles are rejected, and new, saved and edited-saved PDF requests return HTTP 200 with download feedback. These browser checks used an isolated ignored QA database.

The packaged logo's SHA-256 exactly matches the supplied file: `b390a843144556546558d166207f476d7e2197070ec35a1a23064fbbb7da9ac7`. Report generation makes no workbook, OneDrive, external image or current-pricing request for saved snapshots. Existing databases need no migration.

The independently captured Excel fixture contains 216 scenarios ×151 outputs. Default source results match all151 formulas. Workbook issues retained: double masking material adjustment, quantity-driven global adjustments, weekly hire under a daily label, no separate pinning labour and blank B56. Cached empty text and true blank yields remain distinct.

No technical suitability rules were inferred. The next product extension beyond Calculator parity requires authoritative quantity/coverage/thickness/FRL rules not present in the source files. Preserve the current tested formula behavior when adding any new workflow.

Temporary inspection evidence lives in `C:\ESTIMATOR\.workbook-audit` and `.inventory-audit`, outside the application repository. Runtime database, bytecode and distribution archives are intentionally ignored; source XLSM and lock files are not committed. No deployment or branch deletion is authorized by a publication result alone.
